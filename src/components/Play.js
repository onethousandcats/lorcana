import { locations } from "../config/locations";

export default () => {

    return (
        <div
            id={locations.IN_PLAY}
            className="in-play section"
        > 
        </div>
    );
}