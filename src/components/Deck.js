import { locations } from "../config/locations";

export default () => {

    return (
        <div
            id={locations.DECK}
            className={`${locations.DECK} section`}
        >
        </div>
    );
}